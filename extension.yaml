# yaml-language-server: $schema=https://developers.netlify.com/sdk/config-schema.json
config:
  slug: t36-674x-netlify-falkordb-integration
  name: falkordb
  description: "The Data Backbone of Intelligent AI Solutions"
  visibility:
    isPrivate: true

  scopes:
    site: ["read", "write"]
    env: ["read", "write", "delete"]
    user: ["read", "write"]

  use_cjs_shims: false

  ui:
    surfaces:
      - extension-team-configuration
      - extension-top-level-site-configuration
